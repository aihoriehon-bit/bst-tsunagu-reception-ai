# 受付フィードバック確認版（2026-09-14）

- 公開入口は `/wide-desk-preview/`。このフォルダは改善版専用の処理・音声。
- 本番 `/` は従来の `wide-desk-preview/app.js` を使用したまま。確認・承認前に本番の参照先や共有スクリプトを変更しない。
- モデルと名前音声バンクは既存ファイルを参照し、重複生成していない。
- 顔登録は `tsunagu-feedback-identities-v1` に保存。初回は従来の登録を読み込み、保存・削除・変更は比較版のキーだけに適用する。
- 撮影は正面・本人の右・左の3段階。音声指示、安定確認付き3秒カウント、成功表示。中断や失敗では途中データを保存しない。既存登録へ最大12サンプルまで追加できる。
- 検知範囲を全体/中央70%/中央50%/左右半分から指定。ROI外のモニターを避ける補助であり、写真・画面のなりすまし検出ではない。
- 顔の照合は検出した同一フレームから切り出す。処理時間に応じてフレーム有効時間を1800〜6000msに調整。本人照合の距離0.45・競合差0.08・連続3回は変更しない。
- 全身検出入力は最大960px。顔が十分に確認できないときは名前を呼ばず来訪検知だけに使う。
- 会話はローカルな定型状態遷移。用件→担当者→来訪者名→確認。誰でもいい/分からないなら総務。訂正→担当者/自分の名前で修正。
- 実電話、通知、予約登録はしない。完了時にも画面に明記。音声入力はブラウザの認識サービスへ送信される場合があり、初回マイク許可が必要。
- VOICEVOX:春日部つむぎの新規20クリップ。生成時に「取次ぎ」をひらがな指定し、重複読みを修正。音声・かな・時間・ピークは audio/generation.json。

## 検証

主要48テスト（検知、区分別挨拶、名前呼び、マイク切替、会話、公開分離）合格。
実ブラウザ1366×900/390×844で表示・7ターン会話と訂正を確認。
合成映像/疑似識別器で3段階保存・途中キャンセル・本番登録無変更を確認。これは実顔の認証率を測ったものではない。
実カメラで照明・モニター有無・近距離/通常距離・本人/別人/複数人を比較すること。実環境での認証率改善は未測定。

## 確認手順

追加名「まっきぃさん」は名前全体の春日部つむぎ音声をMP3で収録。「まっきぃ」「まっきい」「まっきー」と各カタカナ表記に対応。元の名前バンクは変更せず、この確認版専用の追加リストで照合する。単独の呼び名として登録できる。

1. カメラ開始後「顔・配達登録」。緑枠に背景のモニターが入らないよう範囲選択。
2. 本人同意と名前・読みを確認して3回撮影。普段の距離でも同じ人のサンプルを追加。
3. 顔検知の挨拶後、マイクを許可。「打ち合わせに来ました」→「誰でもいい」→「山田太郎です」→「はい」。訂正も試す。
4. 問題なければユーザーの承認後に本番へ反映。元ページのデータを無断で置き換えない。
